import React, {Component} from 'react';
import appLayout from '../app.layout';

class Product extends Component {
  render() {
    return (
      <div>Product</div>
    )
  }
}

export default appLayout(Product);
